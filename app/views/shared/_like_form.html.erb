<% if user_signed_in? %>
	<div class="like_form">
			<% if current_user.likes?(likeable) %>
				<%= render 'shared/unlike', likeable: likeable %>
			<% else %>
				<%= render 'shared/like', likeable: likeable  %>
			<% end %>
		</div>
<% else %>
	<div class="icon-favs"></div>
<% end %>
